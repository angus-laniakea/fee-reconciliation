# Fee Configuration
# Fees are charged per contract

fees:
  options:
    exchange_fee: 0.55      # Per contract
    clearing_fee: 0.25      # Per contract
    regulatory_fee: 0.02    # Per contract
  futures:
    exchange_fee: 1.38      # Per contract
    clearing_fee: 0.25      # Per contract
    regulatory_fee: 0.02    # Per contract

# S3 Configuration
s3:
  bucket: laniakea-trading
  prefix: exegy-trade-lists/

# Webhook Configuration
webhook:
  url: "https://defaultd6cfaa7b6d7242d1a4d0c0c4915e30.35.environment.api.powerplatform.com:443/powerautomate/automations/direct/workflows/db2b181bce5f42cb93a39caa68e14c21/triggers/manual/paths/invoke?api-version=1&sp=%2Ftriggers%2Fmanual%2Frun&sv=1.0&sig=Je-AEE2OYZM7bLb6BbuQgsD-05gnJvnv3BtOgIsK3f0"
  # Optional: Add custom headers if needed
  # headers:
  #   Authorization: "Bearer your-token"

# Schedule (for reference - actual scheduling done via cron/systemd)
# Runs at end of each trading day
schedule:
  timezone: America/New_York
  time: "17:30"  # After market close
